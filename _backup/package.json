{
  "name": "tupedido",
  "version": "3.0.0",
  "private": true,
  "description": "TuPedido — Plataforma de gestión de pedidos multi-rubro vía WhatsApp",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:report": "npx playwright show-report",
    "dev": "npx http-server . -p 5502 -c-1"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "http-server": "^14.1.1"
  }
}
